# Week of Mon 2026-08-10 to Sun 2026-08-16 -- what exists, and what does not

> Commissioned by Pip 2026-08-09: *"Can you please build me a preview of your
> Current, Planned and Suggested (assuming the first two are incoherent or thin)
> plans for the coming week and whether they're anywhere / jira / tools /
> calendars, both repo and physical?"*
>
> He predicted CURRENT and PLANNED would be thin. **The measurement agrees with
> him, but not for the reason he probably expected.** PLANNED is not empty -- it
> holds **eleven** dated items landing inside these seven days. What it lacks is
> any mechanism that will put a single one of them in front of him. Nine of the
> eleven have no reminder at all. The two that do are reminded by a person, and
> that person is him.
>
> Every date below cites a file, an issue or a run. An unsourced date in a
> planning document is the exact defect this repo spent the weekend cataloguing
> (`docs/POSTMORTEM_SATURDAY_ITEMS_2026-08-08.md`), so none appear here.

---

## 0. The headline, before the detail

**Nothing in this week is in a calendar, a Jira, a milestone or a tool.** The
evidence for each half of that sentence is in section 3, per system.

**pdoom1 does not use Jira and has never used it.** A grep for `jira` across
every `.md`, `.py`, `.gd`, `.yml` and `.json` in the repo returns **zero
matches**. `docs/ROADMAP.md:111-113` states the position deliberately: *"Jira
adoption is deliberately deferred until the project is funded and housed in an
org, at which point it follows that org's work management."* Jira is a
founder-layer thing elsewhere in the estate; it is not a pdoom1 surface. Nothing
in this week should be looked for there.

**GitHub milestones -- which `CLAUDE.md` names as the live "Now" -- contain none
of it.** The four milestones and their due dates
(`gh api repos/PipFoweraker/pdoom1/milestones`, read 2026-08-09):

| Milestone | Due | Open |
|---|---|---|
| Future Releases | 2025-12-30 (**8 months overdue**) | 3 |
| First Contact | 2026-09-29 | 47 |
| Technical Debt Cleanup | 2026-10-30 | 5 |
| Rivals & News | 2026-12-30 | 5 |

**No milestone falls due in August.** `#1061` (due Monday) and `#984` (due this
week) both carry `milestone=none`. The system `CLAUDE.md` designates as the live
roadmap has no opinion about the next seven days.

---

## 1. CURRENT -- in flight right now

### 1a. Eleven open PRs, all awaiting review or merge

None is draft. Oldest is 2 days old, newest 8 hours
(`gh pr list --repo PipFoweraker/pdoom1 --state open`, read 2026-08-09).

| PR | Subject | Opened |
|---|---|---|
| #1194 | changelog + CI: repair 14 false claims that are public right now (#1165) | 08-09 |
| #1192 | test-runner: a timeout is not a test result -- `--timeout` flag (#1181) | 08-09 |
| #1191 | **workshop-2 sealed position** -- the artefact behind this week's C5 bet | 08-08 |
| #1190 | **`docs/design/LEAGUE_CUTOVER_PLAYBOOK.md`** -- and the number blocking a public league schedule | 08-08 |
| #1189 | `force_alarm` drill for the freshness check, whose alarm has never fired | 08-08 |
| #1186 | test-gate: a timed-out run is NO RESULT (#1181) | 08-08 |
| #1185 | ladder: arm the board-key guard, drop `\|\| true` (#1178) | 08-08 |
| #1184 | ladder: arm `check_ladder_bump.py` and prove it can fail (#1178) | 08-08 |
| #1176 | leaderboard: two identity fields -- the Operator-name fix from Saturday Item 1 | 08-07 |
| #1166 | **art share set** -- the missing route from "Pip likes it" to a public surface | 08-07 |
| #1164 | art L2 wave -- 306 images, USD 15.30 | 08-07 |

**Three of these are duplicate pairs.** `#1186` and `#1192` both close `#1181`;
`#1184` and `#1185` both close `#1178`. Two lanes solved the same two problems
independently within three minutes of each other on Friday night. **Somebody has
to pick one of each and close the other**, and no mechanism will do that.

**`#1190` matters more than its position in the list suggests.** The
`LEAGUE_CUTOVER_PLAYBOOK.md` the commission asked about **does not exist on
main** -- it exists only inside this unmerged PR. `coordination#48` (filed by
this seat, addressed to `pdoom1-website`) cites it as the *"full measured
record"* for a timing contract another repo is being asked to build a public
pre-announcement on. **A cross-repo ask is pointing at a file main does not
have.**

### 1b. The Fable cold-open intro lane

`origin/feat/intro-portal-stitch-1112`, last commit `f1d24e1f` at **2026-08-09
16:08 AEST** -- eight hours old, still moving. Working `#1112` (Pip's intro +
poster rulings of 2026-08-04 11:34). Not touched by this document.

### 1c. Workshop 2 -- closed this morning, minute lands today

`coordination#47`. Chaired by `pdoom-data`, all four phases ran, **closed 11:35
AEST 2026-08-09**. Pip was absent by design and, per the chair, *"was not needed
once."* Ten rulings R1-R10. **He reads the minute Monday** -- that is the first
PLANNED item below.

### 1d. What is NOT in flight, contrary to the brief

The commission described the art from the paid run as *"unreviewed with no route
to a public surface."* **The first half is now false and the correction matters,
because it changes what next week is for.**

Pip applied **262 verdicts** to the `an0807` art night, producing **143 keeps**
(PR #1166 body, quoting him directly). The review happened. What has not
happened is the *route*: ADR-0019 mentions publication *"not once"*, so an asset
Pip likes that no game slot demands is never packed and the website has nothing
to pull. `#1166` builds exactly that route and **has sat unmerged since
2026-08-07**.

So the accurate statement is: **the art is reviewed and the route is written and
neither is merged.** The bottleneck moved from Pip's taste to a merge queue, and
`#1093` (*"generation was paid for and the outputs were never reviewed"*) is now
partly stale as filed.

**One figure I could not confirm.** The commission cites a **74.75 USD** run. A
grep for `74.75` across the repo returns nothing. What is measured in
`docs/design/ART_RUN_2026-08-07.md`: hard ceiling **USD 75.50** (line 449, a
module constant), projected total **USD 71.98 = AUD 101.94** at 1.4164 (line
623). `#1164` adds **USD 15.30** for the L2 wave and is unmerged. **I am not
able to source 74.75 and am not going to round toward it.**

---

## 2. PLANNED -- dated, with who committed each

Eleven items. **The "who" column is the load-bearing one**: an obligation
another repo is waiting on behaves nothing like an intention Pip expressed once.

| # | Date | Item | Committed by | Source |
|---|---|---|---|---|
| P1 | **Mon 08-10** | IP / trademark -- take the five-point brief to Australian lawyers | **Pip**, who asked to be forced | `pdoom1#1061` |
| P2 | **Mon 08-10** | Read the Workshop 2 minute | `coordination` G-seat, on Pip's approval of topic + chair | `coordination#47` body |
| P3 | **Week of 08-10** | Half-day audit-mechanics workshop | **Pip**, verbatim ruling 2026-07-27 18:11 | `pdoom1#984` |
| P4 | **Thu 08-13** | Asset provenance: can it be captured retroactively? | **`pdoom1` seat** -- it named itself blocking party | `coordination#32` |
| P5 | **Thu 08-13** | Thursday dev (the working half of the cadence) | Pip, recorded 2026-08-06 | `docs/HANDOVER_2026-08-06_EVENING.md:163` |
| P6 | **Fri 08-14** | Friday push -- *"an ordinary patch day"* | same | same |
| P7 | **Fri 08-14** | P7 falsifier: open-issue count. If >= 208, the weekly close-sweep did not happen | The postmortem pack, self-imposed | `docs/POSTMORTEM_2026-08-07_CAPTURE.md:758` |
| P8 | **Fri 08-14** | P8 falsifier: `grep -n MEASURED docs/art/SEED_ART_COST_MODEL.md` | same | `...:759` |
| P9 | **Fri 08-14** | **The pack's own falsifier** -- if none of P1-P8 started and no issue cites the document, stop producing printed packs | same | `...:761-764` |
| P10 | **Sat 08-15** | Generation spend policy **EXPIRES**: `$100 AUD/day unconstrained` was set *"until 2026-08-15"* | **Pip**, 2026-07-26 | `docs/art/PIXELLAB_OPERATIONS.md:39-41` |
| P11 | **Sun 08-16** | **Workshop 2 C5: four cross-repo bets scored** | Four seats, ruled R6 by the chair | `coordination#47`, chair's closing ledger |

### Notes that change how three of these read

**P7 is already failing, and I measured it today rather than waiting.**
`gh issue list --state open --limit 500` returns **211**. The threshold is 208.
Unless 4+ issues close in the next five days, the falsifier fires: *"the sweep
did not happen and the triage lane should not be re-run until it does."*

**P9 is the one nobody will feel.** It is the only item on the list whose
subject is *the planning apparatus itself*. If it fires, this document is
evidence for the prosecution.

**P11 is not one bet, it is four, and pdoom1's is the standard.** Chair's ruling
**R6**: pdoom1's red-run rule -- *"no guard counts as installed until a RED run
of it has been observed and its run ID recorded"* -- **is adopted as the
standard the other three bets are scored against.** pdoom1's own bet is three
forced red runs, with a stated falsifier
(`docs/workshop-2/position.md:545-552`):

> *"On 2026-08-16, ask for three run IDs -- one per guard -- each showing the
> guard in its FAILING state, each linked from a release artefact. If fewer than
> three exist, the bet was named and not operated, and this seat should be told
> so in Workshop 3 rather than allowed to re-propose it."*

**PR #1189 is that bet's first instalment** and it is unmerged. The bet predicts
**~70%** that at least one alarm path fails on first fire for a reason unrelated
to what it watches. Predicted cost: 2-3 agent-hours, **zero Pip attention**.

### Three asks from the Workshop 2 chair, addressed to Pip

From the closing ledger, `coordination#47`, and **nothing else needs him**:

1. `pdoom-data`'s `main` push trigger is dead. One empty-commit push settles it.
   **Yes/no.** (The chair refused to write to another repo's `main` unattended.)
2. **Class 8 needs an owner**: one check across all three repos that a `main` tip
   carries a completed run -- not installed until a red run is recorded.
3. The four C5 bets are scored 2026-08-16. **Nothing required today.**

### Just outside the window, listed because they shape it

- **2026-08-20 / 08-21** -- falsifiers P1-P6 of the same postmortem pack.
- **2026-08-31** -- the formal 4-way founder-hours review. `#984`'s workshop is
  scheduled *"before"* it; that is the only reason `#984` lands this week.
- **2026-09-04** -- v0.15 ships (`docs/ROADMAP.md`, Monthly Themes table).
- **2026-09-09** -- Manifund deadline (`pdoom1#1061`, citing website #194).

### On W4 / W5 / W6

**They do not exist, and the question contains a collision this repo already
documented.** `docs/GLOSSARY.md:415-427` distinguishes two numbering schemes
that are *"routinely confused"*:

- **WS-\<n\>** = a whole multi-day design workshop producing ADRs. WS-1, WS-2,
  WS-3 (`docs/game-design/WORKSHOP_3_PREP.md`, run Mon 07-27 to Wed 07-29).
- **W\<n\>** = a timeboxed **agenda block inside one workshop day**. On Wed
  2026-07-29: `W0` = PR review, `W1` = colour architecture, `W2` = decorating
  math, `W3` = league cadence, **`W4` = next-epoch planning**.

So **`W4` already happened**, on 2026-07-29, as a block. **There is no W5 and no
W6 anywhere in this repo** -- a scan of `docs/**/*.md` for `\bW[456]\b` returns
only `CLAIM_AUDIT` row labels and `POSTMORTEM` finding labels, which are a third
unrelated use of the same letter.

**The next design workshop is WS-3's successor and it has no name, no number and
no date.** The only workshop with a date attached this week is `#984`'s half-day
audit deep-dive, and "week of 2026-08-10" is a target window Pip wrote a
fortnight ago, not a booking.

---

## 3. Whether they are anywhere -- per system, including the blanks

| System | Holds what | Will it surface anything next week? |
|---|---|---|
| **Repo docs** | P4, P5-P6, P7-P9, P10, P11 all traceable to a file | **No.** No file in this repo runs on a clock. A dated line in a markdown file is read when somebody opens the file |
| **GitHub issues** | P1 (`#1061`), P3 (`#984`), P4 (`coordination#32`), P2/P11 (`coordination#47`) | **No.** No due-date field, no automation, no bot. `#1061`'s date lives in prose and in a comment |
| **GitHub milestones** | **Nothing from this week.** Nearest due 2026-09-29 | **No.** `#1061` and `#984` are both `milestone=none` |
| **GitHub Projects** | **NO VISIBILITY.** `gh project list` failed: token missing `read:project` scope | **Unknown.** I cannot see whether a board exists. Fix with `gh auth refresh -s read:project` if you want this row answered |
| **Jira** | **NOT USED.** Zero occurrences repo-wide; deferred by policy | **No.** Do not look here |
| **`tools/runsheet/`** | 11 printed HTML sheets, **all retrospective or same-day**; latest `sat-2026-08-08-release-status.html`. Nothing dated beyond today | **Only if somebody builds one.** This document's HTML sibling is the first forward-looking sheet in the folder |
| **`tools/` generally** | `print_doc.py` prints; `check_ladder_bump.py`, `sync_version.py` gate commits | **No.** Every gate fires on a commit. **None fires on a date** |
| **CI / GitHub Actions** | `release-sync-monitor.yml` runs daily; freshness checks on release | **No.** They watch artefacts, not the calendar. And per Saturday Item 4, the daily one was reading a decoy file until `#1182` |
| **Physical / paper** | The walkpack and runsheet habit exists (`docs/WALKPACK_2026-08-02.html`) and is the estate's only demonstrated forcing function | **Only if printed.** Which is why deliverable 2 exists |
| **Calendars (Google / phone / paper diary)** | **I HAVE NO VISIBILITY OF ANY CALENDAR.** I cannot read one and cannot write to one | **Unknown to me.** If none of these dates is in a calendar, then section 4 is the whole story. If some are, only Pip knows which |

The **one recurring anchor** I can see documented is **Rektango, Friday 17:30**
(`docs/game-design/WORKSHOP_3_PREP.md`, *"the weekly anchor"*), which puts a hard
stop on **Fri 2026-08-14** -- the same day three falsifiers fall due.

---

## 4. THE GAPS -- what would remind him, per item

**Eleven dated commitments. Two have a reminder mechanism of any kind, and both
of those mechanisms are a human being remembering.**

| # | Date | What would remind him | Verdict |
|---|---|---|---|
| P1 | Mon 08-10 | **Nothing.** An issue with a date in its title and a comment. Already slipped once, 08-03 -> 08-10 | **UNSCHEDULED** |
| P2 | Mon 08-10 | The `coordination` recorder posting the minute. A person-shaped reminder, but a real one | **WEAK -- has an actor** |
| P3 | Week of 08-10 | **Nothing.** Filed 2026-07-27 with a target window and never converted to a date | **UNSCHEDULED** |
| P4 | Thu 08-13 | **Nothing automated.** But `coordination#32` names **pdoom1** the blocking party in public, and another repo is waiting. Social, not mechanical | **WEAK -- externally witnessed** |
| P5-P6 | Thu 08-13 / Fri 08-14 | **Nothing.** A cadence recorded in a handover document | **UNSCHEDULED** |
| P7-P9 | Fri 08-14 | **Nothing.** Three falsifiers inside one document, all requiring somebody to open it on the right day | **UNSCHEDULED** |
| P10 | Sat 08-15 | **Nothing.** The spend policy simply stops being current, silently, mid-sentence in an operations doc | **UNSCHEDULED** |
| P11 | Sun 08-16 | **Nothing in pdoom1.** `coordination#47` is closed; the chair's ledger says the bets are scored, and no seat owns the scoring | **UNSCHEDULED** |

### Why this section is the deliverable

**This repo has two case studies, both from the last nine days, and they are the
same defect.**

**`#1061` slipped 08-03 to 08-10.** The deferral comment is honest about what
would make it slip again, and wrote its own escalation rule: *"If Monday
2026-08-10 passes without lodgement AND funds have arrived, the deferral reason
has expired and this should be escalated rather than moved again."* **That rule
has no executor.** Nothing reads the issue on Tuesday. If Monday passes, the
sentence will be true and unread.

**`#1070` sat correctly diagnosed for seven days and then destroyed a real
board.** From `docs/POSTMORTEM_SATURDAY_ITEMS_2026-08-08.md` Item 3: it named
the exact file and line, `godot/tests/unit/test_leaderboard_properties.gd:31`,
on 2026-07-31. On 2026-08-07 the suite wrote **1,330 files** into Pip's live
profile and took the 2026-07-31 league board from **50 entries to 0**. The
document's own conclusion:

> *"The Saturday question is not 'how do we find these' -- we found it. It is
> what makes a filed, correctly-diagnosed defect get acted on before it costs
> something. Any answer that ends in 'file an issue' has missed the point."*

**Workshop 2 ruled on exactly this and ranked it.** R4: C3 is two findings, and
**C3b (action) outranks C3a (detection)** -- with pdoom1's line adopted verbatim
and evidence from three seats who had read nothing of each other.

**So a plan whose only output is more issues is, by this estate's own ruling
this morning, the wrong answer.** That is why section 5 leads with a mechanism
rather than a task.

### The mechanism that already exists and has not been built

`coordination#43` records **Pip's own idea**, spoken 2026-08-07 16:55, capture
`2026-08-07_165534__57c0a1f4`:

> *"when I'm being harassed about things for the second, third, fourth and fifth
> time, I get more and more and more context, and you make making the decision --
> based off printed material attached to it -- easier and easier, until the
> repetition waste of paper annoys me until I make a decision"*

It escalates **helpfulness**, not urgency -- *"which is much harder to become
numb to."* It is **not scheduled and not scoped**, and its open question (what
happens at the top of the ladder) is his to rule.

**Note the shape:** the mechanism designed to stop asks being forgotten has
itself been sitting unactioned for two days. `coordination#43` carries **no
return date**, which under the estate's own section-5c vocabulary -- *"parking without
a date is abandonment with better manners"* -- renders it **abandoned**.

**And it is not alone.** Of **27 open `coordination` issues**, exactly **two**
carry a return date: `#32` (2026-08-13, live) and `#19` (2026-08-05, **four days
past**). **Twenty-five of twenty-seven render `starved` or `abandoned` under the
vocabulary the same repo wrote.**

---

## 5. SUGGESTED -- three, ranked, one line of argument each

Offered only because sections 1 and 2 are as thin as Pip predicted: **eleven
dates, zero of them scheduled.** Each is weighted toward what he has actually
said he wants -- *"ease off code, ramp UX/player experience, get a little natural
traffic"* (`docs/WALKPACK_2026-08-02.html:194`, quoting his own Friday plan).

### S1. Monday morning, before anything else: put the eleven dates somewhere that fires without him

**Argument:** every other item on this list is downstream of this one, because
the measured failure is not capacity and not noticing -- it is that a correct,
dated, written commitment reaches nobody on its date, twice proven at a cost of a
real league board and a seven-day IP slip. **Cheapest sufficient version:** a
`schedule`d agent, or eleven phone alarms, or one printed sheet on the desk --
the medium matters far less than that it is not a markdown file. **Falsifier:**
on Sun 2026-08-16, count how many of P1-P11 were acted on. Fewer than eight means
whatever was chosen did not work and should be replaced, not tuned.

### S2. Ease off code by *merging* rather than by writing: clear the eleven-PR queue Monday, starting with #1166 and #1189

**Argument:** the queue is the reason "ease off code" has not started -- three of
his stated priorities are already built and blocked on a merge, not on effort.
`#1166` is the entire route from his 262 art verdicts to a public surface (that
is the *natural traffic* lane); `#1189` is the first instalment of the C5 bet
scored on **08-16**, so leaving it costs a public loss in another repo's ledger;
and the `#1181` / `#1178` duplicate pairs are pure waste that grows. **This is
review work, not code work, which is exactly the ramp he asked for.**
**Falsifier:** if clearing the queue takes more than three hours, the PRs were
not review-ready and the problem is upstream of this suggestion.

### S3. Book `#984` as a real half-day, and fold the research/safety unsubtlety into it rather than opening a second workshop

**Argument:** he has said the research/safety mechanics are *"a little unsubtle
for now"* and need **a workshop, not a lane**
(`docs/HANDOVER_2026-08-06_EVENING.md`), and `#984` already asks for a half-day
in this exact week on the adjacent question -- *"what does auditing FEEL like as
a player verb"*, distortion sources, trust-as-a-resource. **Audits and research
integrity are the same distortion surface seen from two ends**; `#1090` (Research
Quality is still a global Plan-screen toggle when the ruling moved it to
project-level) is on the boundary and is currently blocked on a substrate that
does not exist. One session, one ADR, before the **2026-08-31** formal review
that `#984` was written to precede. **Falsifier:** if the two topics need
separate ADRs, they were separate workshops and merging them cost a week --
detectable at the end of the session, not before.

### Explicitly NOT suggested, and why

- **A new tracking document.** P9's falsifier fires on Friday if nothing acts on
  the last one. Producing a twelfth artefact this week would be the finding, not
  the fix.
- **More art generation.** `#33`'s offer has USD 44 of headroom and L2/L3 unrun
  -- but `#1164` is *already generated and unmerged*, and the spend policy
  expires **08-15** anyway. Generating more before merging what exists is the
  same shape as the review backlog `#1093` filed.
- **Anything touching `godot/scripts/ui/cold_open_sequence.gd`.** A Fable lane is
  live on it as of 16:08 today.

---

## 6. What this document does not know

- **Any calendar.** I cannot read Pip's calendar, phone alarms, or paper diary.
  Every "UNSCHEDULED" verdict in section 4 means *"nothing in the repo or on
  GitHub will remind him"* -- **not** *"nothing anywhere will."* If he has these
  eleven dates in a calendar already, section 4 is wrong and section 5's S1 is
  unnecessary. **He is the only one who can close that gap in the evidence.**
- **GitHub Projects.** Token scope blocked the read. Unanswered, not answered
  negatively.
- **Whether the Workshop 2 minute has published.** Due 12:00 AEST 2026-08-09;
  last activity on `coordination#47` was 06:30Z. Not verified at time of writing.
- **The 74.75 USD figure.** Not sourceable in this repo. See section 1d.

---

*Written by the pdoom1 seat, 2026-08-09, against `origin/main` at `7b59166e`.
Reference runsheet named in the commission
(`tools/runsheet/fri-2026-08-07-evening.html`) does not exist; house style taken
from `tools/runsheet/sat-2026-08-08-release-status.html`, which does.*
