# Python cache files
__pycache__/
*.pyc
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
# Keep .spec files for build configuration
# *.spec

# PyInstaller build directories
build/
dist/

# Unit test / coverage
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Virtual environments
venv/
env/
ENV/

# IDE files
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Game logs (preserve structure docs)
logs/
!logs/README.md
!logs/*/.gitkeep
*.log
gamelog_*.txt
emergency_gamelog_*.txt

# Bug reports (user privacy)
bug_reports/
bug_report_*.json

# Runtime data files (should be generated during gameplay)
local_highscore.json
onboarding_progress.json
leaderboard.json
user_privacy.json

# User keybinding customizations (local preferences)
keybindings.json
tutorial_settings.json

# User configuration files (modified during gameplay)
configs/default.json

# Development tools artifacts
tools/temp/
tools/test_output/
tools/*.log

# Godot engine binaries (download locally)
tools/godot/*.exe
tools/godot/*.zip
tools/godot/.import/

# Godot editor data
godot/.godot/
godot/.import/
godot/export_presets.cfg

# Build artifacts (all platform builds - uploaded to GitHub releases)
builds/

# Game runtime logs
game_logs/

# Web exports (Godot HTML5)
web_export/

# Screenshots (keep a few for docs, gitignore bulk)
screenshots/*.png
!screenshots/pdoom_screenshot_20250918_104357.png

# Ruff cache (Python linter)
.ruff_cache/

# Release packages (large files - uploaded to GitHub releases)
*.zip
PDoom-*.zip

# Temporary packaging directory
temp_package/

# GUT JUnit test result artifacts (written by scripts/run_godot_tests.py)
godot/test-results-*.xml
test-results-*.xml
# --summary-file markdown output (same script, #964 sim-tier surfacing)
godot/test-results-*-summary.md
test-results-*-summary.md

# Asset generation pipeline (raw generated images - keep only promoted assets)
art_generated/
generated_icons/
asset_gallery.html
# tools/art_review/review_state.json is TRACKED, deliberately (2026-08-04).
# It was ignored here as "local app state", which it is not: it is 2,700+ curated
# human verdicts and 111 notes -- the provenance record for every art decision this
# project has made. Ignored, it lived on ONE disk with no history, one bad merge run
# from being unrecoverable. It is ~465 KB of JSON: small, diffable, and exactly the
# "index in git, blobs outside" split ADR-0019 is built around.
# Its .bak-* siblings ARE regenerable churn and stay ignored.
# Timestamped backups written by the art-review tools before they touch a
# store. The glob carries a wildcard before .json because a named reviewer's
# store is review_state.<name>.json (2026-08-19, e01a9a0f) -- the old pattern
# matched only the unsuffixed one, so wanasai's backup landed UNTRACKED and
# unignored, one careless `git add -A` from being committed.
tools/art_review/review_state*.json.bak-*
tools/art_review/review_log*.jsonl.bak-*
gallery_verdicts_*.json
# Same deal for the slot picker (tools/art_review/build_slot_picker.py): the
# browser download is a TRANSPORT, not a home. Its home is
# tools/assets/demand/slot_picks.json, which IS tracked.
slot_picks_export_*.json
# Art-review pipeline v2 regenerable artifacts (tools/art_review/). The verdicts
# JSONs (pixellab_verdicts.json, hero_verdicts.json) and the generator/analyzer
# tools ARE tracked -- only these derived outputs are ignored. hero_gallery.html
# also falls under the art_generated/ rule above.
art_source/pixellab_contact_sheet.html
art_generated/hero_gallery.html
art_source/promote_list.txt
art_source/favour_list.txt
art_source/disfavour_dislike_list.txt
art_source/hero_promote_list.txt
art_source/hero_favour_list.txt
art_source/hero_disfavour_dislike_list.txt
# Office-sandbox v2 dev-tool state (per-reviewer feedback; local only). The sandbox
# loader degrades gracefully when absent (godot/scripts/ui/office_floor/office_sandbox.gd).
art_source/sandbox_feedback.json
test_output.png
test_openai_api.py
generate_icons.py

# Third-party visual references (Pip mind-dump 2026-07-16) -- NOT committed (copyright).
# Aesthetic notes derived from these live in docs/game-design/WORLD_AND_LORE.md.
art_prompts/16-july-2-26-dump/

# Music reference tracks -- personal/copyrighted, NEVER committed. Only the
# numeric JSON profiles (tools/music/profiles/, kept via .gitkeep) are tracked.
# See tools/music/README.md. Cover both dir spellings + all audio formats.
tools/music/ref_local/
tools/music/refs_local/
tools/music/**/*.flac
tools/music/**/*.mp3
tools/music/**/*.wav

# Claude Code worktrees (scratch working dirs -- never tracked)
.claude/worktrees/

# Build-stamp freshness markers (tools/write_build_stamp.py) -- generated per
# build with a random name; the .gd is cleaned up but Godot leaves .uid litter.
godot/buildstamp*.gd
godot/buildstamp*.gd.uid
